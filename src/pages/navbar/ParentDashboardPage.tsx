import React from 'react';
import ParentDashboard from '../../components/parent-hud/ParentDashboard';

const ParentDashboardPage: React.FC = () => {
  return (
    <div style={{ paddingTop: '80px' }}>
      <ParentDashboard />
    </div>
  );
};

export default ParentDashboardPage;
